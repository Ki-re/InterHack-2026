# Create CRUD Prompt

Create a CRUD resource for this FastAPI and React app.

Include the SQLAlchemy model, Pydantic schemas, async service functions, FastAPI routes, Alembic migration, typed frontend API client, query hooks, and a simple page or component for list/create/edit/delete. Keep validation explicit and route handlers thin.

Verify migrations and frontend typecheck.
